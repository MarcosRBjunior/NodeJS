# Aprendizados

Registrado ao final de cada sessão de trabalho, quando o usuário disser "fecha a sessão" (ver `CLAUDE.md`, "Regras do processo"). Cada sessão fechada gera um arquivo novo aqui, ex: `2026-09-06-setup-harness.md`, com o que funcionou, o que não funcionou, e qualquer ajuste de processo descoberto na prática — mostrado ao usuário antes de salvar.
